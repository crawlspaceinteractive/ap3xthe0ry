# Checkpoint

- Added `AGENTS.md` to the repository root.
- `AGENTS.md` summarizes the project, run instructions, key files, conventions, and links to `README.md`, `ARCHITECTURE.md`, and `DESIGN.md`.
- No existing `.github/copilot-instructions.md` or other agent customization files were present before this change.

## Notes

- The project is a browser-based PS1-style game engine with a CPU software rasterizer.
- Local development uses `python3 -m http.server 8000` and browser access at `http://127.0.0.1:8000/index.html`.
